apiVersion: v1
kind: Service
metadata:
  name: fog-gateway
spec:
  type: NodePort
  selector:
    app: fog-gateway
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30000
